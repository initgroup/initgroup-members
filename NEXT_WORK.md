# 다음 작업 인계

이 문서는 현재 구현을 안정 지점에서 멈춘 뒤, 다음 요청에서 같은 맥락으로 이어가기 위한 메모입니다. 현재 변경사항은 커밋하지 않은 상태로 보존되어 있습니다.

## 이번에 확인한 직접 오류 원인

- 화면의 반복 `Internal Server Error`는 Oracle 연결 풀 획득 실패였습니다.
- 재현 로그: `DPY-4005: timed out waiting for the connection pool to return a connection`
- 당시 풀 상태가 `opened=0, busy=0`이어서 코드 누수보다는 Oracle Cloud DB·Wallet·네트워크·접속 허용 상태 문제로 판단했습니다.
- 공개 디자인 설정 조회는 DB 장애 시 기본 스킨으로 정상 응답하고, 로그인 화면 렌더링을 막지 않도록 수정했습니다.
- 홈 및 첨부파일의 Oracle 오류는 일반 500 대신 원인을 구분할 수 있는 503 응답으로 정리했습니다.

## 완료한 핵심 범위

- 경영진용 홈: 연도별 입찰 대상, 가중 예상수주, 계획 매출·원가·영업이익, 인력 구성, 부족·과부하 위험, 입찰 판단 대상, 공지 요약
- 연간 사업·인력계획: 실제 투입과 분리된 시나리오, revision 충돌 방지, 사업/인력 작업공간, 드래그앤드롭과 버튼 대체 조작, 월별 M/M·손익 계산, 전체 계획안 트랜잭션 저장과 최종 확정
- 계획 무결성: 프로젝트/인력 기간, 취소 프로젝트, 중복 배치, 실제 투입 용량, 미참여(`SKIP`) 사업 집계 제외, 최신 위험 서명 재확인, 손상된 실제 월배분 데이터의 품질 경고 및 확정 차단
- 실제 프로젝트 투입: 참여회사·내부/협력/프리랜서 인력 검증, 재직·계약기간 검사, 이전 프로젝트 데이터 혼선 방지, 입력 미저장 이탈 경고, 키보드 행 선택, 18자리 금액의 `BigInt` 표시·계산
- 확정 투입 배치 보드: 인력 풀과 프로젝트별 12개월 타임라인, 인력의 프로젝트/월 배치, 기존 배치 기간 이동, 상세 편집기 연결
- 실제 투입 동시수정 보호: 참여회사와 투입정보 수정·삭제 시 `versionToken` 비교, 오래된 화면의 저장·삭제 409 차단
- DB 수명주기: `INIT_SYSTEM_DDL.sql`, idempotent `INIT_SYSTEM_ALT.sql`, 소유자·외부 참조 사전검사를 포함한 `INIT_SYSTEM_DROP.sql`과 `INIT_SYSTEM_TRUC.sql`
- 메뉴/로그인 문구/정보구조를 임직원 일반 포털이 아닌 회사 경영·사업·인력 운영 흐름에 맞게 개편

## 마지막 검증 상태

- `scripts/validate-source.ps1` 통과
  - Python 18개
  - SQL ID 116개
  - DDL/ALT 13개 테이블 계약
  - JavaScript 17개
  - CSS, VS Code JSON, PowerShell
- `git diff --check` 통과
- OpenAPI 생성 성공: 45 paths
- 로컬 서버 확인: `/api/health` 성공, `/app` 200, DB 연결 실패 상황에서도 `/api/site/preferences`가 기본 스킨으로 성공 응답
- 실제 Oracle DB에는 DDL/ALT/DROP/TRUC 또는 테스트 DML을 실행하지 않았습니다.

## 다음 요청에서 이어갈 추가 작업

우선순위 순서입니다.

1. 실제 Oracle 환경 복구 후 `INIT_SYSTEM_ALT.sql` 사전 검토·적용과 계획 저장/확정 통합 테스트
2. `admin-projects`의 발주·수주 금액 API 계약도 계획/투입과 동일한 10진수 문자열로 통일
3. 홈 전용 월별 집계 SQL을 추가해 상세 계획 5,000건·월행 60,000건을 읽은 뒤 집계하는 현재 비용을 제거
4. 계획안 복제·기준안/대안 비교·undo/redo·배치 막대 resize 등 시뮬레이션 편의 기능
5. 실제 DB 데이터가 준비된 상태에서 브라우저로 경영 홈 → 연간 계획 → 저장 → 확정 → 실제 투입까지 전체 시나리오 검증

## 안전 메모

- 사용자 승인 없이 `git add`, `git commit`, `git push`를 실행하지 않습니다.
- `INIT_SYSTEM_DROP.sql`과 `INIT_SYSTEM_TRUC.sql`은 자동 실행하지 않습니다.
- 타 스키마의 비가시 FK를 완전히 사전 확인하려면 DBA 권한의 `DBA_CONSTRAINTS` 점검이 필요합니다.
